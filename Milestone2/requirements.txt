# HDFC MF Assistant — Production API (Railway). Python 3.11. CPU-only. No Streamlit.
# Install: pip install -r requirements.txt
#
# Protobuf / gRPC: pure-Python protobuf implementation avoids many "Descriptors cannot be
# created directly" crashes when mixing chromadb / sentence-transformers wheels.
# Override locally only if you know your stack: PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=cpp

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.2.2+cpu
numpy>=1.26.0,<2
protobuf>=4.25.0,<6
chromadb>=1.0.0,<2
sentence-transformers>=2.2.0,<3
groq>=0.4.0
psutil>=5.9.0
tqdm>=4.65.0
thefuzz>=0.20.0
regex>=2023.0.0
python-dotenv>=1.0.0

fastapi>=0.104.0,<1
uvicorn[standard]>=0.24.0,<1
python-multipart>=0.0.6
pydantic>=2.0.0,<3

# Optional sparse retrieval (disabled by default via USE_BM25=false)
# rank-bm25>=0.2.2
