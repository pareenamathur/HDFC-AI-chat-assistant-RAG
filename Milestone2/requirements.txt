# Streamlit Production Requirements - Memory Optimized
# Lightweight dependencies for stable Streamlit deployment

# Core Streamlit Framework
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0

# Environment Configuration
python-dotenv>=1.0.0

# Vector Database & Embeddings (CPU-only)
chromadb>=0.4.0

# ML Libraries (CPU-only PyTorch)
# Force CPU-only installation to avoid CUDA dependencies
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.0+cpu

# Search & Retrieval (BM25 disabled for memory savings)
rank-bm25>=0.2.2

# AI Model APIs
openai>=1.3.0
groq>=0.4.0

# Essential Utilities (minimal set)
tqdm>=4.65.0
thefuzz>=0.20.0
psutil>=5.9.0

# Sentence Transformers (lightweight model only)
sentence-transformers>=2.2.0

# Text Processing
regex>=2023.0.0
