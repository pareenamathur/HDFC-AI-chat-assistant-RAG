# Streamlit Cloud — CPU-only, minimal surface area.
# Install: pip install -r requirements.txt
#
# CPU-only PyTorch: use +cpu wheels from PyTorch. Pin matches sentence-transformers;
# (torch==2.0.0+cpu is not published for Python 3.12+ — use 2.2.x+cpu locally & on Cloud.)

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.2.2+cpu
numpy>=1.26.0,<2
# Pinned per deployment stability (grpc/chroma may upgrade protobuf unless constrained).
protobuf==3.20.3
streamlit>=1.28.0,<2
python-dotenv>=1.0.0
# 1.x ships reliable wheels on Linux (Streamlit Cloud) and modern environments.
chromadb>=1.0.0,<2
sentence-transformers>=2.2.0,<3
groq>=0.4.0
psutil>=5.9.0
tqdm>=4.65.0
thefuzz>=0.20.0
regex>=2023.0.0

# Optional: only if USE_BM25=true (not recommended on Streamlit Cloud)
# rank-bm25>=0.2.2
