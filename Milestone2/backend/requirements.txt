# Render Free Tier Optimized Backend Dependencies
# Minimal memory footprint for 400MB RAM limit

# Core FastAPI Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Environment Configuration
python-dotenv>=1.0.0

# Vector Database & Embeddings (minimal versions)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ML Libraries (CPU-only, minimal memory)
torch>=2.0.0+cpu
numpy>=1.24.0
pandas>=2.0.0

# Search & Retrieval (BM25 disabled for free tier)
rank-bm25>=0.2.2

# AI Model APIs
openai>=1.3.0
groq>=0.4.0

# Essential Utilities (minimal set)
tqdm>=4.65.0
thefuzz>=0.20.0
psutil>=5.9.0
