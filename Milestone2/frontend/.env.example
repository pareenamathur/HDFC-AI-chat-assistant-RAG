# Copy to .env.local for local dev. On Vercel, set in Project → Settings → Environment Variables.
# Must be the public HTTPS URL of your FastAPI deployment (no trailing slash).

NEXT_PUBLIC_API_URL=https://your-api-host.example.com
